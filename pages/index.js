function Home() {
    const headingStyle = {
        color: '#FFD700',
        textAlign: 'center',
        fontSize: '3rem',
    };

    return <h1 style={headingStyle}>Testing Vercel again</h1>;
}

export default Home;